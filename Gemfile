source :rubygems

gem 'sinatra'

# Switch from Webrick (default web server) to Thin (event websevrer) for production
gem 'thin'

# Heroku Cedar stack supports Procfile
gem 'foreman'

# Heroku toolbelt (since production is deployed to Heroku)
gem 'heroku'
